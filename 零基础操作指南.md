# 零基础操作指南（不会用命令行？看这个就够了）

> 本工具的主流程是：双击打开图形软件，在同一个窗口里完成 **扫描 → 复核 → 安全处理**；需要时可以恢复，不用敲命令。

```
📁 CPU后台整理工具
   ├── 1-扫描.bat     ← 主入口：打开图形软件（扫描本身只读）
   ├── 2-清理.bat     ← 兼容入口：处理已有的可信扫描清单
   ├── 3-恢复.bat     ← 第 3 步：后悔了？点它（一键还原）
   ├── cpu-cleaner.ps1        （程序本体，不用管）
   ├── bloatware-profiles.json （规则库，不用管）
   ├── backups\       （自动备份，恢复时用）
   └── 手动整理方案.md （想自己动手的进阶玩法）
```

---

## 第 1 步：扫描（只查看，绝不改动任何设置）

1. 双击 **1-扫描.bat**，会打开“鼠鼠 Cleaner”图形窗口
2. 点“开始安全扫描”。Windows 会弹出一次用户账户控制，只授权只读清单采集；点【是】可完整读取服务和计划任务
3. 等待扫描完成后，界面会按四组显示结果：推荐/自动安全、可选有影响、已处理、仅观察
4. 如果取消 UAC，软件仍可做有限扫描，但会明确提示信息不完整，服务/任务不会因此获得清理授权

**报告怎么看？**（重点看两个地方）

**① 风险分级汇总** —— 电脑后台整体健康度：

| 级别 | 意思 | 颜色 |
|---|---|---|
| 正常 | 后台很干净 | 绿色 |
| 建议观察 | 有些程序值得留意 | 黄色 |
| 可优化 | 可以清理的后台 | 橙色 |
| 高度建议处理 | 建议尽快处理 | 红色 |

**② 每个进程的风险分** —— 分数越高越可疑：

```
PID   进程名           CPU%  风险分  级别          评分依据
63500 grep           0.49    40   建议观察       非系统目录,无有效签名,同目录x4
```

- **0~29 分** = 正常（比如微软签名、系统目录的程序）
- **30~49 分** = 建议观察（不用急着处理）
- **50~69 分** = 可优化（可以考虑清理）
- **70 分以上** = 高度建议处理

> 看到"高度建议处理"或者报告里标了 `!!` 的预装软件（联想 AI 助手、360、鲁大师这类），就进入第 2 步。

4. 看完按任意键，窗口关闭。

---

## 第 2 步：清理（处理后台程序，先备份，可恢复）

先看懂四组项目，再决定是否处理：

| 分组 | 你会看到什么 | 默认选择 | 你能做什么 |
|---|---|---:|---|
| 推荐/自动安全 | 已实测，并精确命中 `exact` 或 `path` 的安全项目，例如联想通知/诊断任务 | 已勾选 | 保持勾选或取消 |
| 可选有影响 | 精确命中但可能影响功能的项目，例如 `HRWSCCtrl` | 未勾选 | 只有你主动勾选，并确认影响后才能处理 |
| 已处理 | 当前已经 `disabled` 的项目 | 不可选 | 查看状态，不会重复清理 |
| 仅观察 | 只有 `contains` / `regex` 等宽匹配，或资料不完整的项目 | 不可选 | 只能查看，不能执行 |

1. 在扫描结果页点“查看并选择”。自动安全项默认已勾选；不再需要联想电脑管家时，可主动勾选 `HRWSCCtrl`
2. `HRWSCCtrl` 的必要性是 **optional**。若它受 Windows PPL 保护，Cleaner 不会强杀，而会在完整安全复验后只打开联想官方卸载程序；是否继续卸载由你在联想界面决定。只有选择中包含这类 `manual_impact` / 可选有影响项时，才会出现独立二次确认
3. 点“执行已选择项”。如果只选择推荐/自动安全项，确认当前选择后会直接请求管理员 UAC，不弹高影响确认；如果选择中包含可选有影响项，则先通过独立二次确认，再请求管理员 UAC。点【是】才开始执行
4. 软件会按扫描时的同一对象、同一 matcher、同一字段重新验证；身份变化或宽匹配项目会拒绝执行
5. 执行子集同时校验 pending 文件 SHA-256 和已确认的高影响项摘要，防止清单或确认范围被替换
6. 持久动作先备份，执行后再次读取系统状态；打开官方卸载程序这类动作不生成 Cleaner 恢复包，并显示“需要手动处理”，不会冒充卸载成功

**每个动作都会在修改前自动备份**。需要回退时用第 3 步恢复；恢复只接受本工具创建并验证通过的备份，并会再次检查恢复后的状态。

> 注意：扫描阶段的第一次 UAC 只用于完整只读扫描；清理阶段的 UAC 发生在确认当前选择之后。只有选中 `manual_impact` / 可选有影响项时，UAC 前才多一步高影响确认；automatic-only 不弹该确认。两种 UAC 的权限和用途不同。

---

## 第 3 步：恢复（后悔了？一键还原）

1. 双击 **3-恢复.bat**
2. 它先列出你电脑上的所有备份（比如 `20260809_120000`）
3. 输入你想恢复的那个备份名字，回车
4. 弹窗点【是】，程序就会把之前禁用的服务、删掉的自启项和禁用的任务按备份还原，并显示逐项结果

> 备份是按时间自动命名的，选最新的那个就是"恢复到上次处理之前"。

---

## 常见问题

**1. 黑窗口一闪而过，什么都没看到？**
一般是你没来得及看，再双击一次就行。如果每次都是闪一下就消失，右键该文件 → 以管理员身份运行。

**2. 杀毒软件（Windows Defender）弹出警告？**
这是正常的——这个工具会修改系统设置，杀毒软件会提醒。确认文件来源可靠（自己下载的）就可以点"允许"。
如果被误删了：Windows 安全中心 → 病毒和威胁防护 → 保护历史记录 → 找到被删的文件 → 还原。

**3. 报告全是英文看不懂？**
报告主体是中文的（风险分级、特征库命中都是中文），只有 .bat 弹窗提示是英文（Scan=扫描、Clean=清理、Restore=恢复）。对照：
- `Press any key to close` = 按任意键关闭
- `Type backup name` = 输入备份名称
- `Click YES` = 点"是"

**4. 双击 2-清理.bat 没有弹管理员窗口？**
右键 2-清理.bat → **以管理员身份运行**。

**5. 处理完要不要重启电脑？**
**强烈建议重启一次**。有些后台进程（比如联想的推送服务）要重启后才会真正消失。

**6. 报错"不是内部或外部命令"？**
说明文件放的位置不对或被杀毒软件破坏了。把整个文件夹重新拷一遍，或者确认文件都在同一个文件夹里（不要只拷单个 .bat）。

**7. 电脑变卡会不会被清坏？**
工具只允许精确识别的规则进入执行路径，系统核心服务和驱动不在本工具目标内；但任何系统修改都有影响，所以请逐项阅读原因和影响，并保留备份。`HRWSCCtrl` 不是自动安全项，不使用联想电脑管家时也要由你主动确认后再决定。

**8. 自动测试是不是等于已经在我电脑上验收？**
不是。自动测试使用 Mock 或非破坏性夹具，不等于真实 UAC、真实系统状态变化、执行后检查或恢复闭环。实际清理和恢复仍需要人工确认与验收。

**9. 怎么安装桌面图标？**
在工具目录运行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Install-DesktopShortcut.ps1`。它会创建或更新“鼠鼠 Cleaner”快捷方式，固定指向本目录的 GUI，并使用 `assets\shushu.ico`；不会改动其他桌面快捷方式。

---

## 进阶：想用命令行？三条命令

会用命令行的人可以跳过 .bat，直接：

```powershell
# 命令行有限扫描（只读，但服务/计划任务信息可能不完整）
powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode scan -AllowLimited

# 清理（需管理员：开始菜单搜 PowerShell → 右键以管理员身份运行）
powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode clean

# 恢复（需管理员）
powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode restore -BackupDir ".\backups\备份名"
```

待处理清单使用 `pending_schema_version: 3`，并包含 `actions`、`resolved`、`observations`、`suspicious` 四个数组；看到 schema 2 或缺少数组时，请重新扫描，不要手工修改清单继续执行。

更多细节见 README.md 和手动整理方案.md。
