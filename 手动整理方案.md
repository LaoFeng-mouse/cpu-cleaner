# 手动整理方案：电脑 CPU 后台整理（不装软件版）

> 适用人群：不想用脚本、想自己动手的人。
> 全程不安装任何软件，用 Windows 自带功能完成。
> 预计耗时 20~30 分钟。**每做一步都先看清楚再动手，拿不准就不动。**

---

## 第 0 步：先判断这台电脑到底卡不卡、是不是后台在偷跑

按 `Ctrl + Shift + Esc` 打开任务管理器，点顶部"性能"标签：

1. 看 CPU 那一栏的"利用率"曲线
   - 空闲时（没开大软件）如果持续 **30% 以上**，说明有东西在后台偷跑 → 值得整理
   - 如果 10% 以下，说明电脑本身没问题，卡可能是内存不够或硬盘慢，别乱清理
2. 看"进程"标签，点"CPU"列头排序，看谁排在最上面

**核心思路：CPU 高 ≠ 电脑坏，先找到是谁在吃，再决定动不动它。**

---

## 第 1 步：看任务管理器，识别高占用进程

点 CPU 列排序，逐个问三个问题：

| 进程名 | 判断 |
|---|---|
| System / System Idle Process / DWM | 系统核心，别动 |
| svchost.exe（一堆） | 系统服务宿主，正常，别动 |
| Chrome / Edge / 微信 / 钉钉 | 你自己开的，关了就好 |
| **联想/华为/戴尔/惠普 相关**（见第 2 步清单） | 预装全家桶，重点清理对象 |
| **鲁大师 / 驱动精灵 / 2345 / 360 / 金山** | 国产流氓软件，卸载（见第 6 步） |
| 不认识的名字 | 右键 → 打开文件所在位置，看路径：在 Program Files 正常目录里的一般没事；在 Temp、AppData\Roaming 里的要警惕 |

**重点嫌疑：名字像"服务""管家""助手""加速""清理"的，多半是预装流氓。**

---

## 第 2 步：禁用开机启动项（最简单，立竿见影）

1. `Ctrl + Shift + Esc` → 顶部"启动"标签
2. 看"状态"列，"已启用"的就是开机自动加载的
3. 右键 → 禁用，以下这些可以放心禁：
   - 厂商管家/助手/加速器（联想电脑管家、华为电脑管家等——**注意：如果你在用它的功能，如多屏协同，先留着**）
   - 各种"推广"软件
   - 输入法推广、浏览器推广
4. 别禁的：安全软件（Defender 不用管，它是系统自带的）、显卡驱动、声卡驱动

> 备用入口：设置 → 应用 → 启动。

---

## 第 3 步：禁用预装全家桶服务（重点，按厂商找）

1. 按 `Win + R`，输入 `services.msc`，回车
2. 在列表里找下面这些名字（**对照你的品牌找**），双击 → 启动类型改成"禁用" → 点"停止" → 确定

### 联想（最常见，AI 全家桶最狠）
| 服务名 | 是什么 | 能不能禁 |
|---|---|---|
| LeMCPManagerService | AI 助手核心，会拉起 30+ 进程 | ✅ 放心禁 |
| XLSmartService | 联想小天 AI | ✅ 放心禁 |
| LISFService | 广告推送框架（禁了进程还在，重启才消失） | ✅ 放心禁 |
| LenovoServiceAS | 联想应用商店 | ✅ 可禁 |
| GAService | 游戏 AI 服务 | ✅ 可禁 |
| SmartConnect | Ready For 智能助手 | ✅ 放心禁 |
| LnvVCamInstaller | 摄像头安装器 | ✅ 可禁 |
| LenovoVantageService | Vantage 更新 | ⚠️ 想保留官方更新就别禁 |
| LenovoFnAndFunctionKeys | Fn 功能键 | ❌ 别禁，禁了按键失灵 |
| LenovoPcManagerService | 电脑管家（你在用性能模式就留着） | ⚠️ 看情况 |
| HRWSCCtrl | 电脑管家安全组件 | ❌ 禁不掉也正常，别管 |

### 华为
| 服务名 | 是什么 | 能不能禁 |
|---|---|---|
| HwServiceHost | 华为服务 | ⚠️ 多屏协同/一碰传依赖它，用就留 |
| 华为电脑管家 | 设备管理 | ⚠️ 用多屏协同就留，不用可卸载 |

### 戴尔
| 服务名 | 是什么 | 能不能禁 |
|---|---|---|
| Dell SupportAssist / DellUpdateService | 自动更新助手 | ✅ 可禁（手动更新即可） |

### 惠普
| 服务名 | 是什么 | 能不能禁 |
|---|---|---|
| HPAppHelper / HPSupportSolutionsFramework | 支持助手 | ✅ 可禁 |

### 华硕（注意别一刀切）
| 服务名 | 是什么 | 能不能禁 |
|---|---|---|
| AsusUpdateCheck / ASUSSoftwareManager | 更新/推广 | ✅ 可禁 |
| ArmouryCrate | 游戏灯效/性能 | ❌ 游戏用户别禁 |

> 判断口诀：**驱动类（显卡/声卡/WiFi/Fn键/触控板）别动，管家助手类（AI/商店/推送/更新器）可禁。**

---

## 第 4 步：禁用计划任务（藏在后台的定时任务）

1. 按 `Win + R`，输入 `taskschd.msc`，回车
2. 左侧"任务计划程序库"→ 右键 → 查看 → 显示隐藏的任务
3. 找厂商目录（`\Lenovo\`、`\Huawei\`、`\Dell\` 等），或者名字带厂商的
4. 右键 → 禁用。典型：
   - `LenovoMachineFixUser_OOBE_AUTO_Notification`（联想通知）
   - `Lenovo UDC Diagnostic Scan`（联想诊断）
   - `StartLenovoMagiCenterDetecterOnLogin`（联想 MagiCenter 检测）
5. 微软系统自带的任务（`\Microsoft\Windows\...`）**一律别动**

---

## 第 5 步：卸载垃圾/推广软件

1. 设置 → 应用 → 已安装的应用
2. 按"安装日期"或"名称"排序，找这些卸载：
   - 联想：百应（LenovoBaiying）、联想语音、MagiCenter、联想应用商店（如果不用）
   - 国产流氓：鲁大师、驱动精灵、2345 全家桶、360（**在用 360 杀毒防护的就自己权衡**）、金山毒霸、各种"Wifi 助手""壁纸引擎"推广版
   - 各家厂商的"支持助手"（Dell SupportAssist、HP Support Assistant 等，不想要就卸）
3. 卸载完的软件如果还弹"驱动更新""加速"广告，说明有残留服务，回到第 3 步按名字禁掉

---

## 第 6 步：验证效果

1. 重启电脑（**重要：很多服务禁用后要重启才真正消失**）
2. 开机后等 2 分钟，按 `Ctrl + Shift + Esc` 看 CPU 利用率
3. 对比整理前：空闲 CPU 从 30~90% 降到 5% 以下 = 成功
4. 日常使用一周，确认没有功能异常（Fn 键、WiFi、音效、多屏协同等）

---

## 安全守则（先读再动手）

1. **只禁不删**：禁用随时能改回来，删文件/卸载要谨慎
2. **服务可以禁，驱动不要禁**：显卡、声卡、WiFi、触控板、Fn 键相关服务动了会坏功能
3. **拿不准就不动**：宁可留一个无害的后台，也别把系统搞坏
4. **改完重启验证**：功能异常第一时间改回"自动"启动
5. **备份恢复**：想自动备份+一键恢复，用配套脚本（见 README）

---

## 常见问题

**Q：禁了服务会不会影响 Windows 更新/杀毒？**
A：不会。上面列的全是厂商预装软件，跟 Windows 核心无关。Defender、Windows Update 不受影响。

**Q：禁了联想电脑管家，Fn 键会失灵吗？**
A：不会，Fn 键是独立的 LenovoFnAndFunctionKeys 服务管，别禁它就行。

**Q：整理完还是卡怎么办？**
A：说明瓶颈可能不在 CPU 后台，而是：内存不够（任务管理器看内存占用）、硬盘是机械盘/快满了、或者 CPU 太老。这种是硬件问题，清理软件解决不了。
