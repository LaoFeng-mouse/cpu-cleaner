# 命令行入门操作指南（从零开始，一步步教你）

> 适用：完全没用过命令行的人。
> 本指南从"怎么打开命令行"教起，每一步都写清楚，照着做就行。
> 全程只需要**复制粘贴**，不需要自己打命令。

---

## 第 0 步：先认识命令行窗口

命令行（也叫"黑窗口"）是一个让电脑执行指令的地方：
- 你输入一行文字，按回车，电脑就执行
- 它长这样：黑底白字（或蓝底白字）
- **不用怕，按本指南做不会弄坏电脑**

本工具的三种操作，对应三行命令，都是复制粘贴：

| 想做什么 | 复制这行命令 |
|---|---|
| 扫描（只查看） | `powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode scan` |
| 清理（要管理员） | `powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode clean` |
| 恢复（后悔了） | `powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode restore -BackupDir ".\backups\备份名"` |

---

## 第 1 步：打开命令行（两种方法，选一个）

**方法 A（最快）**
1. 按键盘上的 `Win + R`（Win 是带 Windows 图标的键）
2. 屏幕左下角弹出"运行"小窗口
3. 输入 `powershell` 三个字
4. 按回车 → 出现蓝色窗口 = 成功

**方法 B（一步步点）**
1. 点屏幕左下角"开始"按钮（Windows 图标）
2. 直接打字：`powershell`
3. 搜索结果里出现"Windows PowerShell"（黑色图标）
4. 点它 → 出现蓝色窗口 = 成功

> 注意：蓝色窗口 = 普通权限（够扫描用）
> 黑色窗口 = 管理员权限（清理才需要，见第 3 步）

---

## 第 2 步：进入工具文件夹（关键一步）

工具在文件夹里，命令也必须在这个文件夹里运行。

1. 先找到工具文件夹的位置，比如在 U 盘里：`E:\CPU后台整理工具`（每个人的盘符可能不同，看你的 U 盘或下载位置）
2. 在命令行窗口里输入：`cd /d E:\CPU后台整理工具` 然后回车
   - `cd` = 进入文件夹
   - `/d` = 切换盘符
   - **把 E: 换成你实际的盘符**（D: / E: / F: 都行）
3. 回车后如果没报错，就进去了（可以输入 `dir` 回车，能看到文件列表）

> 如果工具文件夹在别的位置，右键文件夹 → 复制路径（或点文件夹地址栏复制），把命令里的路径换成你的。

---

## 第 3 步：执行操作

### 扫描（只查看，安全）
1. 在命令行窗口里**右键** = 粘贴
2. 粘贴这行，回车：
   ```
   powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode scan
   ```
3. 等 10~30 秒，看报告（有中文说明：风险分级、每个进程的风险分）

### 清理（需要管理员，会弹窗点"是"）
1. **换一个管理员窗口**：开始菜单搜"powershell" → 右键"Windows PowerShell" → **以管理员身份运行** → 点"是"
2. 重复第 2 步进入文件夹
3. 粘贴这行，回车：
   ```
   powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode clean
   ```
4. 看到项目列表后，输入 `all` 回车 = 全部处理
5. 每个项目处理完显示"验证通过"

### 恢复（后悔了）
1. 管理员窗口，进入文件夹
2. 先看备份名：输入 `dir backups` 回车，记下一个文件夹名（如 `20260809_120000`）
3. 粘贴这行，把"备份名"三个字换成实际的：
   ```
   powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode restore -BackupDir ".\backups\20260809_120000"
   ```
4. 回车执行

---

## 常见问题

**Q：粘贴没反应 / 右键没菜单？**
Windows 10 以后的命令行都支持右键直接粘贴。如果不行，点窗口左上角图标 → 编辑 → 粘贴。

**Q：提示"无法加载文件"或"禁止运行脚本"？**
命令里已经有 `-ExecutionPolicy Bypass`，一般不会。如果还报错，把整行重新完整复制（特别是别漏了开头的 `powershell` 和结尾的 `-Mode xxx`）。

**Q：提示"不是内部或外部命令"？**
说明当前不在工具文件夹里，重新执行第 2 步（`cd /d 你的路径`）。

**Q：为什么我看到的命令和 .bat 文件一样？**
完全一样！`.bat` 文件就是把这些命令包好、双击就运行。如果你觉得命令行太麻烦，直接用 `1-扫描.bat`、`2-清理.bat`、`3-恢复.bat` 三个文件，效果相同。

---

## 速查卡（打印/截图保存）

```
Win+R → 输入 powershell → 回车           ← 打开命令行
cd /d E:\CPU后台整理工具                  ← 进入工具文件夹(改盘符)
powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode scan      ← 扫描
powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode clean     ← 清理(管理员)
powershell -ExecutionPolicy Bypass -File cpu-cleaner.ps1 -Mode restore -BackupDir ".\backups\备份名"  ← 恢复(管理员)
```
